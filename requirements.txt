# Runtime dependencies of the report generator.
#
# The report-generation surface (REG + dates -> xlsx) needs exactly these at run
# time. Copy the repository (or just report_generator/ plus this file),
# `pip install -r requirements.txt`, and put the directory holding
# report_generator/ on the import path.

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# HTTP requests + SRF API client
requests>=2.31.0
srf_client

# Reverse geocoding (postcode lookup)
geopy

# File locking (weather cache + vehicles.json capacity-ledger write-back)
filelock>=3.12.0

# Environment variables (.env loading in the CLI)
python-dotenv>=1.0.0

# Report generation
openpyxl>=3.1.0
xlsxwriter>=3.1.0
tqdm>=4.65.0
