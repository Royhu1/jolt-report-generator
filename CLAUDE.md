# CLAUDE.md

> **Role: core repo** (see `~/.claude/rules/repo-roles.md`) — this repository holds the JOLT
> Excel report generator (`report_generator`), its offline tests and its docs. Experiments,
> deliverables and research live in the workspace `../JOLT_Report`.
>
> Conventions are self-contained so collaborators get them too: `AGENTS.md` carries the
> generated global conventions (all AI tools read it); `.claude/rules/project.md` holds this
> repository's deltas and overrides. Refresh the generated block after global rule changes:
> `/sync-agent-rules`.

## Conventions (self-contained copy)

@./AGENTS.md

## Project-specific rules

@./.claude/rules/project.md

## Structure

@./README.md
