# Copy to .env and fill in. Never commit the real file.

# Required: SRF telematics platform API key
SRF_API_KEY=

# Optional: only needed for the OpenWeather back-fill post-step, which is NOT
# part of default report generation (default generation makes zero paid API calls).
OPENWEATHER_API_KEYS=
